import { UnifiedResponseInterceptor } from './unified-response.interceptor';

describe('UnifiedResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new UnifiedResponseInterceptor()).toBeDefined();
  });
});
